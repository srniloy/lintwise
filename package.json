{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "setup-frontend": "cd frontend && npm install && npm run dev",
    "setup-backend": "cd backend && npm install && npm run start:dev"
  },
  "dependencies": {},
  "devDependencies": {}
}