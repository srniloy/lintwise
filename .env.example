# lintwise frontend — environment variables (Vite, build-time)
# Copy to `.env` for local dev or set as a GitHub secret (LINTWISE_VITE_API_URL) for CI.

# URL the browser uses to reach the lintwise API server.
VITE_API_URL=http://localhost:5000/api/v1
